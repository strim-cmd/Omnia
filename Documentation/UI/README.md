# Omnia UI Specification

These documents are the implementation specification extracted from the supplied Omnia UI concept.

Start here:
1. `DESIGN_SYSTEM.md`
2. `COMPONENTS.md`
3. `SCREENS/CHAT.md`

Implementation instruction for an AI coding agent:

> Read these files before modifying SwiftUI UI code. Treat them as the visual contract. Reuse existing architecture and assets. Do not redesign the interface or invent major components. When the specification does not provide an exact measurement, use native iOS/SwiftUI conventions and keep the visual hierarchy consistent with the reference.
