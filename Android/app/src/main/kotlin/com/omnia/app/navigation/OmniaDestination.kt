package com.omnia.app.navigation

/** Every destination in the M1 navigation shell. */
enum class OmniaDestination(val route: String) {
    CHAT("chat"),
    PROVIDERS("providers"),
    SETTINGS("settings"),
    ABOUT("about"),
}
